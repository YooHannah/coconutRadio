<!--index.wxml-->
<view class="container">
  <!-- <view class="userinfo">
    <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称 </button>
    <block wx:else>
      <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" background-size="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </block>
  </view> -->
  <image bindtap="bindViewTap" class="userinfo-avatar" src="{{backgroundimg}}" background-size="cover"></image>
   <view class="usermotto"> 
     <text class="user-motto">{{motto}}</text> 
  </view>
  <view class='section'>
  <!-- <input placeholder='请输入标题关键字' value='{{keywords}}' bindblur="bindKeyInput">{{keywords}}</input> -->
  <button bindtap='bindJump' data-val='1'>查询</button>
  <button bindtap='bindJump' data-val='2'>我想听最新一期</button>
  <button bindtap='bindJump' data-val='3'>树洞</button>
  </view>
  <!-- <view class="usermotto"> -->
    <!-- <text class="user-motto">{{motto}}</text> -->
    <image bindtap="bindViewTap" src="{{backgroundimg}}" background-size="cover"></image>
   <!-- </view>  -->
</view>
